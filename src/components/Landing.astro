<div class="landing-hero">
  <div class="branding">
    <div class="red-block"></div>
    <h1 class="glitch-text" data-text="PENGUI.DEV">PENGUI.DEV</h1>
  </div>
  
  <div class="status-terminal">
    <p class="line">>> SYSTEM_CHECK: <span class="highlight">OPTIMAL</span></p>
    <p class="line">>> APPLICANT_ID: <span class="highlight">PENGUI_2026</span></p>
    <p class="line">>> DESTINATION: <span class="highlight">KTH_STOCKHOLM</span></p>
  </div>

</div>

<style>
  .landing-hero {
    height: 100vh; /* 占满全屏 */
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    background-color: var(--mirror-white);
    position: relative;
    overflow: hidden;
  }

  .red-block {
    width: 60px;
    height: 60px;
    background-color: var(--famicom-red);
    margin-bottom: 1rem;
    animation: rotateBlock 2s infinite ease-in-out;
  }

  .glitch-text {
    font-size: 5rem;
    font-weight: 900;
    color: var(--text-main);
    letter-spacing: -2px;
    margin: 0;
  }

  .status-terminal {
    margin: 2rem 0;
    font-family: 'Courier New', monospace; /* 模拟终端 */
    text-align: left;
    border-left: 2px solid var(--famicom-red);
    padding-left: 1rem;
  }

  .line {
    font-size: 0.9rem;
    margin: 0.3rem 0;
    color: #666;
  }

  .highlight {
    color: var(--famicom-red);
    font-weight: bold;
  }

  .enter-btn {
    margin-top: 2rem;
    padding: 1rem 3rem;
    background-color: var(--famicom-red);
    color: white;
    font-weight: 900;
    text-decoration: none;
    letter-spacing: 2px;
    transition: transform 0.2s, box-shadow 0.2s;
    /* 红白机按键感 */
    box-shadow: 0 5px 0 #821d1a;
  }

  @keyframes rotateBlock {
    0% { transform: rotate(0deg); }
    50% { transform: rotate(90deg); background-color: var(--famicom-gold); }
    100% { transform: rotate(180deg); }
  }
</style>